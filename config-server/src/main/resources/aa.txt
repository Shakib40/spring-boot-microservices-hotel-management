server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Shakib40/spring-boot-microservices-config-hotel-management
          default-label: main
          search-paths: /
          clone-on-start: true

 
