server:
  port: 8888

spring:
  application:
    name: config-server
  profiles:
    active: native
  cloud:
    config:
      server:
        # git:
        #   uri: https://github.com/shakil9/config-repo
        #   default-label: main    # or master
        #   search-paths: /
        #   clone-on-start: true
        native:
          search-locations: classpath:/config
