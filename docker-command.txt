

docker compose --profile dev down


docker compose --profile dev up -d --build
docker compose --profile prod up -d --build

docker compose --profile dev build config-server discovery-server

docker-compose --profile dev logs

docker-compose --profile dev logs config-server
docker-compose --profile dev logs discovery-server


// for logs
docker logs -f config-server --tail 200


docker compose --profile dev up -d discovery-server
docker logs -f discovery-server --tail 200


docker compose --profile dev up -d discovery-server
docker logs -f discovery-server --tail 200



docker compose up -d --build



#MYSQL_DATABASE

docker exec -it auth-mysql mysql -u root -p
SHOW DATABASES;
USE auth_db;
SHOW TABLES;
SELECT * FROM users;





  mysql:
    image: mysql:8.0
    container_name: mysql
    environment:
      MYSQL_ROOT_PASSWORD: "Shakib@40"
      MYSQL_DATABASE: hotel_management_db
    ports:
      - "3306:3306"
    networks:
      - spring-net
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h localhost -p$${MYSQL_ROOT_PASSWORD} --silent"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 20s

  redis:
    image: redis:7-alpine
    container_name: redis
    command: ["redis-server", "--appendonly", "yes"]
    ports:
      - "6379:6379"
    networks:
      - spring-net
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 10s